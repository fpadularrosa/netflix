const User = require('../models/User');

module.exports = {
    updateCtrl: async (req, res) => {
        try {
            
        } catch (error) {
            
        }
    },
    getContent: async (req, res) => {
        try {
            
        } catch (error) {
            
        }
    }
}